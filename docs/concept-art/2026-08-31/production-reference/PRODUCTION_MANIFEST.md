# Production Reference Manifest, 31 August 2026

This batch registers the selected Bloom mechanized-host concept using the project-standard master
plus implementation-companion pattern. Both packets are grounded in the implemented
`ABloomMechanizedEnemy` class and preserve the selected hero image as a separate source artifact.

| Sheet | Packet status | Primary production use | Blocking gate |
| --- | --- | --- | --- |
| `bloom-mechanized-host-production-system-v2.png` | Design review | Neutral view set, scale, removable layers, materials, sockets, collision and damage zones, progression, representative motion, export structure | Resolve human-canopy versus JACK-head lineage and replace generated views with measured orthography |
| `bloom-mechanized-host-implementation-mapping-v1.png` | Technical review | Rigid controls, tendril splines, animation notifies, runtime tuning, VFX, masks, LODs, naming, and Unreal handoff | Approve head lineage, validate rigid-motion parity, and establish the five-chain runtime budget |

The preserved `bloom-mechanized-host-production-reference-v1.png` packet is superseded by the V2
master. It is lineage only and is not an active implementation authority.

The image sheets are production direction, not finished Unreal or Blender assets. Their JSON
sidecars are authoritative for exact values, source hashes, conflicts, and acceptance checks.

Validation command from the exact project root:

```powershell
Set-Location -LiteralPath 'C:\Users\James\Documents\Unreal Projects\Ginnungagap'
python tools\production_reference_pipeline.py all
```
