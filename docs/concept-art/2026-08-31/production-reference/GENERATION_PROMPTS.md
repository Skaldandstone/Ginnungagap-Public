# Image Generation Prompt Record

Built-in OpenAI image generation was used with the project-local selected hero concept as the
subject reference. The two final images follow the established master plus implementation-companion
sheet pattern. Exact implementation data, authority, conflicts, and acceptance gates live in the
sibling `.production.json` packets.

## Bloom Mechanized Host Production System V2

Reference: `bloom-mechanized-host-hero-concept-v1.png`.

Prompt summary: create the canonical landscape production-system board for the selected massive
industrial host. Preserve the small recessed human face behind damaged curved glass, asymmetric
one-sided Bloom takeover, off-white shell, graphite mechanisms, black-violet fibers, cyan nodes,
and exactly five unequal late-state tendrils. Include neutral front, strict side, back, and
three-quarter views; human scale and current capsule; exploded removable layers; rigid hierarchy;
growth socket map; material and mesh zones; collision and damage zones; four progression states;
representative locomotion, attack, damage, and death silhouettes; Blender collections; Unreal
destination; export checks; and visible blocking gates. Use deliberate broad geometry, limited
surface noise, readable labels, no environmental floor or wall texture, and no watermark.

The generated views remain visual proposals. They must be replaced by captures from one measured
graybox before the orthography gate can pass.

## Bloom Mechanized Host Implementation Mapping V1

References: the selected hero concept and the V2 master sheet.

Prompt summary: create the established technical companion board with motion silhouettes, rigid
Control Rig hierarchy, exact growth sockets, five root-mid-tip tendril chains and parameters,
animation notify points, runtime tuning ranges, infection progression and reveal thresholds,
collision and damage zones, separate Niagara layers, material channels, render masks, LOD
silhouettes, naming prefixes, Unreal destination, and the false-signal readability rule. Keep the
base rig, growth meshes, tendrils, occupant, glass, collision, and VFX independently addressable.

The local handoff label was corrected to `AN_Bloom_MechanizedHost_*`. The sibling JSON is
authoritative for every exact identifier and numeric value.

## Superseded Sheet

`bloom-mechanized-host-production-reference-v1.png` remains preserved for lineage, but its packet is
superseded by `GGP.Enemy.Bloom.MechanizedHost.ReferenceV2` because it did not contain the full
category-required sheet set or a separate implementation map.
